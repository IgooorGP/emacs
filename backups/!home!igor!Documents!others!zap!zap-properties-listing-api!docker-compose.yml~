version: '3'
services:

  # main app container
  web:  
    build: .
    volumes:
     - .:/zap
    ports:
     - "5000:5000"
    env_file: .env
    command:
      ["./docker-entrypoint.sh"]